# Task Queue — Sibway Logistics

> Формується в Project-чаті (PM-роль). Claude Code бере в роботу лише
> перше завдання зі статусом `TODO`, зверху вниз. Не змінювати порядок
> без узгодження з PM. Формат кожного завдання — нижче. Синхронізовано
> з `sibway_project_final_artifacts.md` (backlog, розділ 10) станом на
> 2026-09-15.

## Як користуватись цим файлом

1. PM (Project-чат) формулює завдання і додає його в кінець списку
   зі статусом `TODO`.
2. Claude Code перед початком роботи читає `docs/CLAUDE.md`,
   `docs/RULES.md` і релевантні джерела істини (`docs/DECISIONS.md`,
   `docs/PROJECT_FACTS.md` тощо) — так само, як і раніше.
3. Claude Code бере **перше зверху** завдання зі статусом `TODO`,
   міняє статус на `IN PROGRESS`, виконує план → QA → чекає дозволу
   власника на commit/push, як завжди.
4. Після виконання: статус → `DONE`, додається короткий підсумок
   (файли, commit SHA, дата). Завдання не видаляється — лишається як
   історія, доки PM не почистить файл.
5. Якщо завдання заблоковане (бракує рішення власника, дані тощо) —
   статус `BLOCKED` + причина. Claude Code переходить до наступного
   `TODO`.

## Формат завдання

```
### [ID] Коротка назва

- Статус: TODO | IN PROGRESS | BLOCKED | DONE
- Додано: YYYY-MM-DD
- Контекст: сторінка/функція/проблема, репозиторій
- Бізнес-ціль: навіщо це потрібно
- Технічна ціль: що саме зробити
- Дозволений scope: точні файли/каталоги
- Заборони: що не чіпати
- Критерії приймання: перевірювані умови
- Перевірки: які QA-кроки виконати
```

---

## Черга завдань

### [T-001] Видалити preview-середовище (preview.sibway.com.ua)

- Статус: DONE
- Підсумок (2026-09-22): власник видалив піддомен `preview` на
  Cityhost разом із файлами preview і його DNS-запис. Production
  (`sibway.com.ua`) не зачеплено — `https://sibway.com.ua/uk/index.html`
  → 200. Відхилення від критерію приймання, погоджене власником
  (варіант 1): у DNS лишається wildcard-запис `*.sibway.com.ua`,
  тому `preview.sibway.com.ua` досі резолвиться й показує лише
  стандартну заглушку Cityhost (без контенту Sibway, без ризику
  дублікатів); wildcard свідомо не видаляється. Документацію
  оновлено: `docs/DEPLOYMENT.md` (§9), `docs/CHANGELOG.md`,
  `docs/CLAUDE.md`, `docs/PROJECT_TODO.md` (B6); `docs/SITE_STRUCTURE.md`
  змін не потребував. Гілка `docs/t-001-preview-removed`, commit/PR —
  див. історію Git.
- Додано: 2026-09-15
- Контекст: сайт повністю перенесено на production
  (`sibway.com.ua`), `preview.sibway.com.ua` більше не потрібен як
  staging — публічно відкритий, без Basic Auth/noindex, ризик
  дублікат-контенту й стороннього доступу.
- Бізнес-ціль: прибрати непотрібну публічну поверхню й ризик SEO
  дублікатів; спростити інфраструктуру.
- Технічна ціль: видалити файли preview-піддомену на хостингу
  (Cityhost), прибрати або дезактивувати DNS A-запис/піддомен
  `preview.sibway.com.ua`, за потреби відкликати чи дати
  протерміновуватись SSL-сертифікату preview. Оновити
  `docs/DEPLOYMENT.md` і `docs/SITE_STRUCTURE.md` (репозиторій
  вимог) — прибрати згадки preview як активного середовища,
  зафіксувати дату й причину видалення.
- Дозволений scope: конфігурація хостингу/DNS для
  `preview.sibway.com.ua`; `docs/DEPLOYMENT.md`,
  `docs/SITE_STRUCTURE.md`, `docs/CHANGELOG.md` у репозиторії вимог.
- Заборони: не чіпати production (`sibway.com.ua`), не видаляти
  DNS/hosting-налаштування для основного домену, не видаляти
  git-історію чи сам runtime-код (`sibway-website-` залишається
  джерелом правди для деплою, навіть без активного preview).
- Критерії приймання: `preview.sibway.com.ua` більше не резолвиться
  або повертає помилку з'єднання; production не зачеплено;
  документація оновлена.
- Перевірки: `curl -I https://preview.sibway.com.ua` (очікується
  помилка DNS/з'єднання, не 200); production критичні URL — без змін
  (`curl -I https://sibway.com.ua/uk/index.html` → 200).

---

### [T-002] Search Console — моніторинг 3–7 днів після запуску

- Статус: DONE
- Підсумок (2026-09-22): read-only перевірка GSC виконана, звіт —
  `docs/reports/T-002_gsc_2026-09-22.md`. Sitemap успішний (34),
  проіндексовано 34; ключові UA-сторінки й блог у індексі, canonical
  збігається, breadcrumbs валідні. Не проіндексовано 2 (не блокери):
  `/index.html` (редирект-файл з noindex) і `/pl/services/warehouse.html`
  (проскановано, ще не проіндексовано).
- Додано: 2026-09-15
- Контекст: sitemap подано 15.09.2026, 34 URL, без помилок.
- Бізнес-ціль: впевнитись, що індексація стабільна і немає
  blocker-ів для головної, блог-хабу й статей.
- Технічна ціль: перевірити статус `Sitemaps` (успішний, 34
  discovered), URL Inspection для UA home, blog hub і 3 статей,
  Breadcrumbs enhancement (якщо зʼявиться), збіг Google-selected і
  user-declared canonical для Blog URL.
- Дозволений scope: read-only перевірка в Google Search Console, без
  змін коду чи конфігурації.
- Заборони: не вносити зміни без окремого рішення за результатами.
- Критерії приймання: короткий звіт власнику — статус sitemap, URL
  Inspection результати, знайдені проблеми (якщо є).
- Перевірки: —

---

### [T-003] Read-only legacy cleanup audit

- Статус: DONE
- Підсумок (2026-09-22): read-only HTTP-аудит production (без SFTP,
  без видалень), звіт — `docs/LEGACY_CLEANUP_AUDIT.md`. Критично:
  відкритий старий обробник `contact_form.php` (відповідає «запит
  надіслано» на GET), живий старий сайт `index.php` (старий GA-тег і
  сторонній трекер), публічні `.DS_Store`. ≈19 МБ legacy у `img/`,
  `lib/`, `scss/`. Запропоновано 5 cleanup-хвиль — чекають рішення
  власника. Повний inventory потребує експорту списку файлів із
  FileZilla.
- Додано: 2026-09-15
- Контекст: на production лишились legacy-файли старого сайту
  (`index.php`, `contact_form.php`, `fonts/`, `lib/`, `scss/`,
  `css/bootstrap.min.css`, старі img) — навмисно не видалені після
  cutover.
- Бізнес-ціль: підготувати безпечне й контрольоване прибирання
  legacy без ризику зламати щось на проді.
- Технічна ціль: порівняти production tree з 54-file runtime
  manifest, знайти всі legacy-only шляхи, перевірити HTTP exposure
  (`index.php`, `contact_form.php`, Bootstrap, архіви/логи/backup),
  класифікувати кожен файл: leave for now / candidate for removal /
  security exposure candidate / current runtime / unknown—owner
  decision. Сформувати cleanup waves — **без видалення**.
- Дозволений scope: read-only інвентаризація хостингу; звіт як новий
  документ (наприклад, `docs/LEGACY_CLEANUP_AUDIT.md`) у репозиторії
  вимог.
- Заборони: жодних видалень файлів на хостингу в межах цього
  завдання.
- Критерії приймання: повний inventory + класифікація +
  запропоновані cleanup-хвилі, готові до рішення власника.
- Перевірки: —

---

### [T-004] Ручна перевірка ключових конверсійних сценаріїв

- Статус: DONE
- Підсумок (2026-09-24): власник особисто виконав ручну перевірку —
  форма (Formspree), CTA-кнопки й мобільна навігація на
  `sibway.com.ua` працюють без зауважень.
- Додано: 2026-09-15
- Контекст: форма (Formspree), CTA та мобільна навігація ще не
  перевірені "живим" тестовим зверненням у чистому браузері після
  cutover на основний домен.
- Бізнес-ціль: впевнитись, що основний канал лідогенерації реально
  працює на production.
- Технічна ціль: надіслати тестову заявку через форму на
  `sibway.com.ua` (UA/EN/PL), перевірити, що лист доходить на
  `sibway@ukr.net`; перевірити CTA-кнопки та мобільне меню на
  реальному пристрої.
- Дозволений scope: ручне тестування, без змін коду.
- Заборони: —
- Критерії приймання: тестова заявка отримана; CTA і мобільна
  навігація працюють без зауважень; знайдені проблеми зафіксовані як
  нові завдання.
- Перевірки: —


### [RES-002] Внутрішнє інтерв'ю: підтвердити операційну спроможність ADR / температурний режим / негабарит

- Статус: DONE
- Підсумок (2026-09-25): власник підтвердив напряму — у Sibway є
  стабільна операційна спроможність через партнерів для всіх трьох
  сценаріїв: ADR (небезпечні вантажі), температурний режим
  (рефрижератори), негабарит. Зафіксовано в пам'яті проєкту
  (`claude/sibway-business-profile`). Розблоковує SEO-006/SEO-007 —
  ці три сценарії тепер МОЖНА використовувати на сайті як реальні
  можливості (текст і формулювання — окреме питання підготовки
  контенту, без вигаданих строків/цін/гарантій).
- Додано: 2026-09-25
- Контекст: зовнішній план (Perplexity) для SEO/AEO/GEO пропонує сторінки
  та FAQ про ADR-вантажі, температурний режим і негабарит для напрямку
  Чехія. У профілі компанії (внутрішнє інтерв'ю 19.09) зафіксовано лише
  загальне «беремо все, що підходить під профіль» — без окремого
  підтвердження, що Sibway стабільно організовує саме ADR/температурні
  перевезення через партнерів.
- Бізнес-ціль: не публікувати на сайті непідтверджені твердження про
  спроможність (ризик: клієнт звернеться з ADR-вантажем, а Sibway не
  зможе його закрити).
- Технічна ціль: власник підтверджує чи спростовує (коротко, текстом):
  чи є партнери для ADR, чи є досвід температурних перевезень, чи є
  досвід негабариту, і за яких умов. Результат — короткий запис у
  `claude/sibway-business-profile` (пам'ять) з позначкою
  «підтверджено власником».
- Дозволений scope: питання власнику, фіксація відповіді. Без змін
  коду/контенту сайту в межах цієї задачі.
- Заборони: не публікувати сторінки/FAQ про ADR/температуру/негабарит
  до отримання відповіді.
- Критерії приймання: є пряма відповідь власника (підтверджено /
  спростовано / частково, з умовами) для кожного з трьох сценаріїв.
- Перевірки: —

---

### [SEO-005] Service/BreadcrumbList schema на service-сторінках

- Статус: TODO
- Додано: 2026-09-25
- Контекст: техаудит 22.09 (`docs/reports/SITE_AUDIT_2026-09-22.md`, P1)
  вже фіксував відсутність Service/BreadcrumbList schema на сторінках
  послуг. Зовнішній план (Perplexity) підтверджує цей самий пріоритет
  і уточнює допустимий набір: Organization, WebSite, WebPage, Service,
  BreadcrumbList, ContactPoint (за потреби LocalBusiness); без
  фейкових рейтингів/відгуків/цін/строків, розмітка має відповідати
  видимому тексту сторінки.
- Бізнес-ціль: коректні структуровані дані для пошуку та AI Overviews
  без ризику порушення Google's structured data policies.
- Технічна ціль: додати/перевірити JSON-LD Service + BreadcrumbList на
  `import-poland.html`, `import-germany.html`, `import-czechia.html`,
  `transport.html`, `customs.html`, `warehouse.html`, `audit.html`
  (uk/en/pl) — за наявною практикою з блог-сторінок (JSON-LD вже
  додано на 18 сторінках раніше, PR #16).
- Дозволений scope: `<script type="application/ld+json">` блоки на
  вказаних сторінках усіх трьох мов. Видимий текст/макет/форми — без
  змін.
- Заборони: не змінювати видимий контент сторінок; не додавати
  AggregateRating, Review, offers/price, FAQPage без окремого дозволу.
- Критерії приймання: Rich Results Test — валідно, без помилок;
  розмітка відповідає видимому H1/breadcrumb на сторінці.
- Перевірки: Rich Results Test для кожної зміненої сторінки.

---

### [SEO-006] Аудит трьох готових route pages проти AEO-чекліста + внутрішня перелінковка

- Статус: TODO
- Додано: 2026-09-25
- Контекст: `import-poland.html`, `import-germany.html`,
  `import-czechia.html` вже опубліковані й затверджені. Зовнішній план
  (Perplexity) пропонує AEO-критерії (перший абзац відповідає прямо на
  питання, H1→H2→H3, FAQ проти реальних заперечень клієнта, а не
  заради schema) та перелінковку кожної route page на FTL/LTL/палети/
  customs/кейси. Це аудит-задача — не редагування контенту.
- Бізнес-ціль: зрозуміти конкретний розрив між затвердженими
  сторінками й AEO-критеріями, перш ніж просити дозвіл на правки.
- Технічна ціль: для кожної з трьох сторінок (uk) звірити: чи перший
  абзац прямо відповідає «що це за послуга, для кого, за яким
  напрямком»; чи є FAQ і чи вони унікальні по напрямках (не
  скопійовані одна з одної); чи є внутрішні посилання на FTL/LTL/
  customs/кейси. Результат — короткий звіт-таблиця (сторінка →
  розрив → пропозиція), без правок самого контенту.
- Дозволений scope: read-only аналіз опублікованих сторінок; новий
  звіт у `docs/reports/`.
- Заборони: жодних змін видимого контенту/форм/макета сторінок у
  межах цієї задачі — рішення про правки ухвалює власник окремо.
- Критерії приймання: звіт готовий, кожен пункт розриву має конкретну
  пропозицію дії.
- Перевірки: —

---

### [SEO-007] Пріоритизація supporting-контенту: Польща/Німеччина спочатку, Чехія — третьою чергою

- Статус: TODO
- Додано: 2026-09-25
- Контекст: бізнес-профіль (внутрішнє інтерв'ю) ставить Чехію в tier 2
  нарівні з Бельгією/Нідерландами/Румунією/Балтією/Туреччиною, тоді як
  Польща й Німеччина — tier 1. Зовнішній план (Perplexity) трактував
  усі три напрямки як рівнозначну «трійку» — це розходиться з
  реальними пріоритетами бізнесу.
- Бізнес-ціль: не розпорошувати ресурс на глибокий контент (FAQ,
  гайди, кейси) для напрямку, який не є пріоритетним, поки Польща й
  Німеччина не отримали повний supporting-шар.
- Технічна ціль: у `05_SEO_Content_Map` (Google Sheets, Marketing
  Operating System) і в будь-якому контент-плані supporting-матеріалів
  зафіксувати чергу: 1) Польща, 2) Німеччина, 3) Чехія — інші
  tier-2/3 напрямки (Бельгія, Нідерланди, Румунія, Балтія, Туреччина)
  окремих landing/supporting-сторінок поки не отримують.
- Дозволений scope: `05_SEO_Content_Map` (Google Sheets) — колонка
  пріоритету; координація з подальшими задачами контент-плану.
- Заборони: не створювати нові route/supporting-сторінки для
  напрямків поза Польщею/Німеччиною/Чехією без окремого рішення.
- Критерії приймання: `05_SEO_Content_Map` відображає чергу 1-2-3.
- Перевірки: —
