# Session Handoff — Sibway Logistics

## 1. Призначення

Оперативний стан проєкту для старту нової AI/Claude Code-сесії, щоб швидко
відновити контекст без читання історії чату. Документ **не замінює**
`RULES.md`, `DECISIONS.md`, `PROJECT_FACTS.md`, `CONTACTS_USAGE.md` чи
`SEO_CONTENT_ROADMAP.md` — при розбіжності пріоритет мають вони.

## 2. Репозиторії

| Репозиторій | URL | Призначення | Актуальний стан (2026-09-08) |
|---|---|---|---|
| Контент і документація | [https://github.com/Zsuff/sibway-logistics-website](https://github.com/Zsuff/sibway-logistics-website) | Правила, рішення, бізнес-факти, бренд-платформа, SEO-roadmap, контентні чернетки, task briefs, handoff | `main`, HEAD `2fe6783`, робоче дерево чисте, синхронізовано з `origin/main` (0 попереду/позаду); з часу попереднього handoff додано 6 комітів: видалення стрей-файлу (`beaa28b`), `PRIVACY_CONTROLLER_SOURCE_OF_TRUTH.md` (`4c6cc8a`), `DATA_PROCESSING_MAP.md`+`COOKIE_INVENTORY.md` (`df3f3b6`), UA/EN/PL Privacy Policy source-of-truth документи (`826fd35`, `fb2fef0`, `2fe6783`) |
| Сайт | https://github.com/Zsuff/sibway-website- | Статичний HTML/CSS/JS сайт UA/EN/PL, SEO-реалізація, assets | `main`, HEAD `0656f99`, синхронізовано з `origin/main`; є чотири незакомічені untracked `.DS_Store` (корінь, `uk/`, `en/`, `pl/` — не додавати в git); з часу попереднього handoff додано 4 коміти: GA4 (`G-CN7867WYLT`, прямий `gtag.js`) + інформаційний cookie-notice (`6ee3c22`), UA/EN/PL Privacy Policy content sync (`55a649b`, `217a249`, `0656f99`) |

## 3. Джерела істини

1. `docs/RULES.md` — правила, межі змін, безпека, Git.
2. `docs/DECISIONS.md` — затверджені рішення власника.
3. `docs/PROJECT_FACTS.md` — підтверджені бізнес-факти.
4. `docs/CONTACTS_USAGE.md` — контакти та CTA.
5. `docs/SEO_CONTENT_ROADMAP.md` — SEO-пріоритети, URL-стратегія.
6. Окремо погоджений task brief або approved copy — точні межі конкретної задачі.
7. `docs/SITE_STRUCTURE.md` — фактична структура двох репозиторіїв.
8. `docs/PROJECT_TODO.md` — робочий список; звіряти з документами вище.

## 4. Погоджені бізнесові рішення

- Sibway Logistics — зовнішній логіст для B2B-компаній, які потребують
  перевезень між Україною та Європою.
- Слоган: «Коли логістики більше, ніж часу — передайте її нам».
- Метрики: 7+ років, 7500+ перевезень, 4000+ клієнтів, 40+ країн Європи.
- Контакти — використовувати лише з `PROJECT_FACTS.md` та `CONTACTS_USAGE.md`.
- Графік офісу: Пн–Пт, 9:00–18:00.
- Підтримка 24/7 — сервісна опція, не графік офісу.
- Пріоритетні країни: Польща, Німеччина, Чехія, Румунія, Туреччина.
- Туреччина: Стамбул, Ізмір, Гебзе, Адана, Газіантеп, Маніса.
- Не публікувати без нового підтвердження: ціни, строки доставки, митні
  ставки, юридичні гарантії, сертифікати, ліцензії, страхування, GPS,
  власний автопарк, непідтверджені кейси/відгуки/маршрути.

## 5. Що завершено

- Оновлено UA/EN/PL title і CTA на сервісних сторінках.
- Нормалізовано структуру прикладу у PL-версії customs.
- Виправлено позначення `24 м³` на UA transport (`3658870`).
- Оновлено meta description `en/terms.html` (`9c14a00`, `2939b31`).
- Синхронізовано підрозділ cookies у `uk/privacy.html` (`91877c4`).
- Уніфіковано структуру `pl/contacts.html` (`912cce3`).
- Оновлено hero головної та `services.html` під позиціонування
  «зовнішній логіст» (`43217ea`).
- Заповнено `about.html` у UA/EN/PL (`43217ea`).
- Оновлено географію Туреччини у transport UA/EN/PL (`364d60a`).
- Оновлено footer disclaimer після запуску About (`f824742`).
- Створено SEO roadmap (`8a807dd`), `PROJECT_FACTS.md` (`da77f8e`) і
  актуалізовано `SITE_STRUCTURE.md` (`253ffd3`) та `PROJECT_TODO.md`
  (`a95eb0b`).

### Завершено 2026-09-04

- CRO-пакет контактних сторінок UA/EN/PL: уніфікована SLA-обіцянка
  (1 година в робочий час), placeholder у полі опису вантажу, точна
  підводка над формою, якір `#quote-form` для нижнього CTA (`749e4f3`).
- До форми заявки додано приховане поле `language` (`uk`/`en`/`pl`) для
  тріажу Formspree-заявок; коректну доставку значення підтверджено
  тестовими заявками з усіх трьох мовних форм (`d8b0d13`).
- Скорочено EN/PL `meta description`/`og:description` на `about.html` до
  ≤160 символів (`5ea639f`).
- Уточнено `docs/CHANGELOG.md` щодо реальної статичної HTML/CSS/JS
  архітектури сайту; застарілий запис 2026-08-20 лишено як історичний
  (`764d7cb`).
- Синхронізовано `content/{ua,en,pl}/_about.md` як короткі контентні
  паспорти — SEO-поля, текст «Хто ми», цінності та CTA — з чинним
  `about.html`; географія й вантажі не дублюються, є лише посилання на
  `PROJECT_FACTS.md` (`92429ea`).

### Завершено 2026-09-08

- Власник погодив продуктове рішення щодо аналітики: GA4 прямим `gtag.js`
  (не GTM), без UniTalk/Nextel/Google Ads/Meta Pixel/goo.gl; інформаційний
  cookie-notice, що не керує й не блокує GA4 (не CMP, не consent banner).
- У сайт-репозиторії реалізовано й запушено GA4 (`G-CN7867WYLT`) на 34
  HTML-файлах та інформаційний cookie-notice на 33 мовних сторінках, разом
  із footer-кнопкою повторного відкриття (`6ee3c22`). Ручна локальна
  функціональна/a11y-перевірка дефектів не виявила.
- Видалено помилковий документаційний артефакт `content/en/privacy.md`
  (`beaa28b`).
- Створено чотири внутрішні privacy-документи як джерела правди для
  майбутнього юридичного рев'ю: `PRIVACY_CONTROLLER_SOURCE_OF_TRUTH.md`
  (`4c6cc8a`), `DATA_PROCESSING_MAP.md` + `COOKIE_INVENTORY.md` (`df3f3b6`).
- Власник погодив робочі UA/EN/PL редакції Privacy Policy (11 розділів,
  дата 8 вересня 2026); закомічено як source-of-truth: UA (`826fd35`), EN
  (`fb2fef0`), PL (`2fe6783`).
- Після послідовного read-only gap-аналізу кожної мови повністю
  синхронізовано `<div class="prose">` у `uk/privacy.html` (`55a649b`),
  `en/privacy.html` (`217a249`) і `pl/privacy.html` (`0656f99`) з
  погодженими текстами; GA4/cookie-notice-блоки не зачіпались у жодному з
  трьох цих комітів.
- **Це синхронізація контенту в Git, не production deployment** —
  `sibway.com.ua` досі обслуговує старий сайт; post-deploy технічна
  перевірка (чекліст у `COOKIE_INVENTORY.md`) не виконана.

## 6. Відкриті задачі

1. **`terms.html` UA/EN/PL** — зараз placeholder; потребує юридично
   погодженого тексту; не вигадувати юридичні положення. (P1-06,
   `PROJECT_TODO.md`; Етап A, `SEO_CONTENT_ROADMAP.md`)
2. **`privacy.html`** — контент синхронізовано в Git у трьох мовах
   (`55a649b`, `217a249`, `0656f99`) на основі погоджених
   `PRIVACY_POLICY_{UA,EN,PL}_SOURCE_OF_TRUTH.md`. Це **не** production
   deployment і **не** заміна юридичного рев'ю: внутрішні джерела прямо
   позначені як «не юридичний висновок». `docs/PROJECT_TODO.md` (P1-07)
   досі має статус TODO — не оновлено після content-sync, потребує
   окремого підтвердження власника (той самий патерн розбіжності, що й
   P1-08).
3. **`contacts.html`** — read-only UX/CRO-аудит проведено; мінімальний
   погоджений пакет реалізовано й запушено (`749e4f3`, `d8b0d13`).
   `docs/PROJECT_TODO.md` (P1-08) досі має статус TODO — не оновлено
   після реалізації, потребує окремого підтвердження статусу.
4. **`about.html`** — фінальне рев'ю власником на живому сайті ще не
   відбулося, тому пункт лишається відкритим. Проміжно виконано:
   read-only UX/content/SEO-аудит, скорочення EN/PL meta description
   (`5ea639f`) і синхронізацію content-паспортів (`92429ea`). Правки
   видимого тексту сторінки — лише за конкретним зворотним зв'язком
   власника.
5. **SEO route-сторінки** — почати з keyword research Польща ↔ Україна та
   Німеччина ↔ Україна; не створювати URL до дослідження попиту, SERP і
   погодження структури. (P1-10)

Додатково з `PROJECT_TODO.md`: P0-02 (реальне надсилання заявок із форм) і
P0-05 (резервна копія старого сайту) лишаються TODO — статус не
підтверджено жодним документом.

## 7. Робочий протокол

- Одна сесія = одна завершувана задача.
- Перед нетривіальною задачею прочитати джерела істини, релевантні задачі.
- Спочатку коротко підтвердити мету, межі, файли, план і ризики.
- Змінювати лише явно дозволені файли.
- Не створювати commit, push, PR або deploy без прямого окремого дозволу
  власника.
- Не додавати `.DS_Store`.
- Після виконання звітувати стисло: статус, файли, перевірки,
  commit/push, лише невирішені ризики.
- Не наводити великі логи, повні diff або хід міркувань, якщо вони не
  потрібні для прийняття рішення.

## 8. Шаблон старту наступної сесії

```text
Працюємо над Sibway Logistics.

Режим: [аудит / дослідження / контент / реалізація / рев'ю].

Спочатку прочитай:
- docs/CLAUDE.md
- docs/RULES.md
- docs/DECISIONS.md
- docs/PROJECT_FACTS.md
- docs/CONTACTS_USAGE.md
- docs/SEO_CONTENT_ROADMAP.md
- docs/SITE_STRUCTURE.md
- docs/PROJECT_TODO.md
- docs/SESSION_HANDOFF.md
- [релевантний task brief або approved copy]

Потім коротко підтвердь:
1. мету;
2. дозволені файли;
3. джерела істини;
4. план і ризики.

Не змінюй файли, не роби commit, push, PR або deploy, доки я окремо
не підтверджу наступний крок.
```

## 9. Наступна рекомендована дія

> Провести read-only release-readiness / post-deploy planning: звести
> докупи чекліст із `docs/COOKIE_INVENTORY.md` (post-deploy перевірка
> GA4/cookie-notice), стан `terms.html`/`privacy.html`/`about.html` і
> відкриті P0/P1 пункти `PROJECT_TODO.md` в єдиний план готовності до
> заміни старого сайту на `sibway.com.ua`, без внесення змін.

Обрано саме цю дію: privacy-цикл (GA4, cookie-notice, UA/EN/PL sync)
щойно завершено в Git, і природний наступний крок — не повторне
редагування policy-тексту, а зведення критеріїв готовності до реального
deploy. `.DS_Store` housekeeping (корінь, `uk/`, `en/`, `pl/`) — окрема
тривіальна технічна задача, яку можна виконати незалежно в будь-який
момент, без залежності від release-readiness планування.

## 10. Відкриті питання (розбіжності джерел)

### Закрито

- `docs/CHANGELOG.md` — застарілий запис 2026-08-20 про
  React/Vite/TypeScript вирішено через `764d7cb`: додано коригувальний
  запис 2026-09-04; історичний запис навмисно лишено, але він не є чинною
  настановою.

### Відкрито

- `docs/PROJECT_TODO.md`, P1-08 (`contacts.html` CRO), досі має статус
  TODO, хоча роботу реалізовано й запушено (`749e4f3`, `d8b0d13`).
  Потрібне окреме рішення власника: оновити статус у `PROJECT_TODO.md`
  або залишити TODO до формального прийняття CRO-пакета.
- `docs/PROJECT_TODO.md`, P1-07 (`privacy.html`), досі має статус TODO,
  хоча content-sync реалізовано й запушено (`55a649b`, `217a249`,
  `0656f99`). Той самий патерн, що й P1-08 — потребує окремого рішення
  власника.
- GA4 (`G-CN7867WYLT`) та інформаційний cookie-notice реалізовані в Git
  (`6ee3c22`), але **не верифіковані на продакшні** — новий сайт ще не
  замінив старий на `sibway.com.ua`. Post-deploy чекліст зафіксовано в
  `docs/COOKIE_INVENTORY.md`.
- UA/EN/PL Privacy Policy content синхронізовано в Git, але **не пройшов
  юридичного рев'ю** — усі чотири internal source-of-truth документи
  прямо позначені як «не юридичний висновок». Потрібне окреме залучення
  юриста перед тим, як вважати текст остаточним.
