# Session Handoff — Sibway Logistics

## 1. Призначення

Оперативний стан проєкту для старту нової AI/Claude Code-сесії, щоб швидко
відновити контекст без читання історії чату. Документ **не замінює**
`RULES.md`, `DECISIONS.md`, `PROJECT_FACTS.md`, `CONTACTS_USAGE.md` чи
`SEO_CONTENT_ROADMAP.md` — при розбіжності пріоритет мають вони.

## 2. Репозиторії

| Репозиторій | URL | Призначення | Актуальний стан (2026-09-09) |
|---|---|---|---|
| Контент і документація | [https://github.com/Zsuff/sibway-logistics-website](https://github.com/Zsuff/sibway-logistics-website) | Правила, рішення, бізнес-факти, бренд-платформа, SEO-roadmap, контентні чернетки, task briefs, handoff | `main`, HEAD `09861a4`, робоче дерево чисте, синхронізовано з `origin/main` (0 попереду/позаду); з часу попереднього handoff-запису (`500c624`) додано 4 коміти: `docs/CLAUDE.md` доповнено + новий `docs/DEPLOYMENT.md` — release/deployment-процес і approval gates (`1ba848a`), підтвердження кейсу Вінниця → Варшава, страхування на повну вартість і відстеження в реальному часі як D-010 у `DECISIONS.md`+`PROJECT_FACTS.md` (`5fe5d32`), синхронізація формулювання customs-кейсу з кодом (`25d51e2`), рішення D-011 про стабілізацію шрифтів і CTA (`09861a4`) |
| Сайт | https://github.com/Zsuff/sibway-website- | Статичний HTML/CSS/JS сайт UA/EN/PL, SEO-реалізація, assets | `main`, HEAD `d2a55b8`, робоче дерево чисте, синхронізовано з `origin/main`; untracked `.DS_Store` більше немає — додано в `.gitignore` (`183a016`); з часу попереднього handoff додано 5 комітів: `.gitignore` для `.DS_Store` (`183a016`), публікація погодженого юртексту `terms.html` UA/EN/PL і прибирання footer-плейсхолдера (`a5be88f`), кореневий `DEPLOYMENT.md` з посиланням на нормативний release-процес у репозиторії вимог (`4a3d152`), service-aware форма контактів і CTA-роутинг `?service=` на сторінках послуг (`23aba1e`), стабілізація рендерингу шрифтів і уніфікація CTA-стилів (`d2a55b8`, задокументовано як D-011) |

## 3. Джерела істини

1. `docs/RULES.md` — правила, межі змін, безпека, Git.
2. `docs/DECISIONS.md` — затверджені рішення власника.
3. `docs/PROJECT_FACTS.md` — підтверджені бізнес-факти.
4. `docs/CONTACTS_USAGE.md` — контакти та CTA.
5. `docs/SEO_CONTENT_ROADMAP.md` — SEO-пріоритети, URL-стратегія.
6. Окремо погоджений task brief або approved copy — точні межі конкретної задачі.
7. `docs/SITE_STRUCTURE.md` — фактична структура двох репозиторіїв.
8. `docs/PROJECT_TODO.md` — робочий список; звіряти з документами вище.
9. `docs/DEPLOYMENT.md` — release/deployment-процес і approval gates
   (Local → GitHub → Preview → Production, ролі репозиторіїв, rollback,
   secrets policy).

## 4. Погоджені бізнесові рішення

- Sibway Logistics — зовнішній логіст для B2B-компаній, які потребують
  перевезень між Україною та Європою.
- Слоган: «Коли логістики більше, ніж часу — передайте її нам».
- Метрики: 7+ років, 7500+ перевезень, 4000+ клієнтів, 40+ країн Європи.
- Контакти — використовувати лише з `PROJECT_FACTS.md` та `CONTACTS_USAGE.md`.
- Графік офісу: Пн–Пт, 9:00–18:00.
- Підтримка 24/7 — сервісна опція, не графік офісу.
- Пріоритетні країни: Польща, Німеччина, Чехія, Румунія, Туреччина.
- Туреччина: Стамбул, Ізмір, Гебзе, Адана, Газіантеп, Маніса.
- Не публікувати без нового підтвердження: ціни, строки доставки, митні
  ставки, юридичні гарантії, сертифікати, ліцензії, страхування, GPS,
  власний автопарк, непідтверджені кейси/відгуки/маршрути.
- Підтверджений кейс перевезення: Вінниця, Україна → Варшава, Польща;
  12 палет; 4 800 кг; 24 м³; строк 3 дні; без затримок на кордоні.
  Підтверджені послуги: страхування вантажу на повну вартість,
  відстеження вантажу в реальному часі (D-010). Це підтвердження не
  поширюється на інші кейси, маршрути чи умови страхування.

## 5. Що завершено

Повна історія з комітами — `docs/CHANGELOG.md`. Тут лишається лише
короткий підсумок для швидкого старту сесії.

### До 2026-09-04 (коротко)

Title/CTA на сервісних сторінках, географія Туреччини, SEO roadmap,
`PROJECT_FACTS.md`, footer disclaimer, `about.html` UA/EN/PL. Деталі —
`docs/CHANGELOG.md`.

### 2026-09-04 — 2026-09-08 (коротко)

CRO-пакет форми заявки (`749e4f3`, `d8b0d13`), content-паспорти
`about.html` (`92429ea`), GA4 прямим `gtag.js` + інформаційний
cookie-notice (`6ee3c22`), чотири внутрішні privacy-документи
(`4c6cc8a`, `df3f3b6`), UA/EN/PL Privacy Policy source-of-truth
(`826fd35`, `fb2fef0`, `2fe6783`) і синхронізація контенту
`privacy.html` (`55a649b`, `217a249`, `0656f99`) — **синхронізація в
Git, не production deployment**. Деталі — `docs/CHANGELOG.md`.

### 2026-09-08 — 2026-09-10 (поточний цикл)

- Release/deployment-процес визначено: `docs/CLAUDE.md` доповнено,
  `docs/DEPLOYMENT.md` доданий (`1ba848a`); короткий `DEPLOYMENT.md` у
  сайт-репо (`4a3d152`).
- Юридичний текст `terms.html` опубліковано в UA/EN/PL (`a5be88f`) —
  без source-of-truth документа чи запису в `DECISIONS.md` (відкрито,
  розділ 10).
- Service-aware форма контактів і CTA-роутинг `?service=` (`23aba1e`,
  `25d51e2`).
- D-010: кейс Вінниця → Варшава, страхування на повну вартість,
  відстеження в реальному часі (`5fe5d32`).
- D-011: стабілізація рендерингу шрифтів (`Manrope`/`Inter` замість
  невстановлених `TT Norms`/`Gilroy`), уніфікація CTA hero/cta-band
  (`d2a55b8`, `09861a4`). **Post-deploy перевірка Windows 11 / Chrome 128
  ще не виконана** (відкрито, розділ 6).
- Homepage quote-form textarea: додано placeholder UA/EN/PL (`37c2bbf`).
- Документація синхронізована (`66dcdf8`, `14c954e`); прибрано
  дублювання й застарілі твердження в `docs/` — шість файлів перенесено
  в `docs/archive/`, `SEO_CONTENT_ROADMAP.md` і `SITE_STRUCTURE.md`
  оновлено, `PROJECT_FACTS.md` актуалізовано, новий запис у
  `docs/CHANGELOG.md`.

## 6. Відкриті задачі

1. **`terms.html` UA/EN/PL** — юридичний текст опубліковано в коді
   (`a5be88f`), footer-плейсхолдер прибрано. `docs/PROJECT_TODO.md`
   (P1-06) досі має статус TODO «блокується відсутністю юртексту» — не
   оновлено після публікації. На відміну від Privacy Policy, немає
   окремого source-of-truth документа чи запису в `docs/DECISIONS.md`,
   що явно підтверджує саме цей текст як погоджений власником і
   юридично перевірений — не вважати це остаточним без такого
   підтвердження. (P1-06, `PROJECT_TODO.md`; Етап A, `SEO_CONTENT_ROADMAP.md`)
2. **`privacy.html`** — контент синхронізовано в Git у трьох мовах
   (`55a649b`, `217a249`, `0656f99`) на основі погоджених
   `PRIVACY_POLICY_{UA,EN,PL}_SOURCE_OF_TRUTH.md`. Це **не** production
   deployment і **не** заміна юридичного рев'ю: внутрішні джерела прямо
   позначені як «не юридичний висновок». `docs/PROJECT_TODO.md` (P1-07)
   досі має статус TODO — не оновлено після content-sync, потребує
   окремого підтвердження власника (той самий патерн розбіжності, що й
   P1-08).
3. **`contacts.html`** — read-only UX/CRO-аудит проведено; мінімальний
   погоджений пакет реалізовано й запушено (`749e4f3`, `d8b0d13`).
   Додатково реалізовано service-aware форму й CTA-роутинг `?service=` з
   чотирьох сторінок послуг (`23aba1e`). `docs/PROJECT_TODO.md` (P1-08)
   досі має статус TODO — не оновлено після жодної з цих реалізацій,
   потребує окремого підтвердження статусу.
4. **`about.html`** — фінальне рев'ю власником на живому сайті ще не
   відбулося, тому пункт лишається відкритим. Проміжно виконано:
   read-only UX/content/SEO-аудит, скорочення EN/PL meta description
   (`5ea639f`) і синхронізацію content-паспортів (`92429ea`). Правки
   видимого тексту сторінки — лише за конкретним зворотним зв'язком
   власника.
5. **SEO route-сторінки** — почати з keyword research Польща ↔ Україна та
   Німеччина ↔ Україна; не створювати URL до дослідження попиту, SERP і
   погодження структури. (P1-10)
6. **Post-deploy перевірка D-011** — обов'язкова ручна перевірка футера
   на Windows 11, Chrome 128, масштаб 100%, `uk/index.html`, плюс
   клавіатурний фокус на CTA в hero та cta-band; ще не виконана
   (`docs/DECISIONS.md`, D-011).

Додатково з `PROJECT_TODO.md`: P0-02 (реальне надсилання заявок із форм) і
P0-05 (резервна копія старого сайту) лишаються TODO — статус не
підтверджено жодним документом.

## 7. Робочий протокол

- Одна сесія = одна завершувана задача.
- Перед нетривіальною задачею прочитати джерела істини, релевантні задачі.
- Спочатку коротко підтвердити мету, межі, файли, план і ризики.
- Змінювати лише явно дозволені файли.
- Не створювати commit, push, PR або deploy без прямого окремого дозволу
  власника.
- Не додавати `.DS_Store`.
- Після виконання звітувати стисло: статус, файли, перевірки,
  commit/push, лише невирішені ризики.
- Не наводити великі логи, повні diff або хід міркувань, якщо вони не
  потрібні для прийняття рішення.

## 8. Шаблон старту наступної сесії

```text
Працюємо над Sibway Logistics.

Режим: [аудит / дослідження / контент / реалізація / рев'ю].

Спочатку прочитай:
- docs/CLAUDE.md
- docs/RULES.md
- docs/DECISIONS.md
- docs/PROJECT_FACTS.md
- docs/CONTACTS_USAGE.md
- docs/SEO_CONTENT_ROADMAP.md
- docs/SITE_STRUCTURE.md
- docs/PROJECT_TODO.md
- docs/SESSION_HANDOFF.md
- [релевантний task brief або approved copy]

Потім коротко підтвердь:
1. мету;
2. дозволені файли;
3. джерела істини;
4. план і ризики.

Не змінюй файли, не роби commit, push, PR або deploy, доки я окремо
не підтверджу наступний крок.
```

## 9. Наступна рекомендована дія

> Провести read-only release-readiness / post-deploy planning: звести
> докупи чекліст із `docs/COOKIE_INVENTORY.md` (post-deploy перевірка
> GA4/cookie-notice), обов'язкову Windows 11 / Chrome 128 перевірку з
> D-011, стан `terms.html`/`privacy.html`/`about.html`/`contacts.html` і
> відкриті P0/P1 пункти `PROJECT_TODO.md` (включно з розбіжностями
> P1-06/P1-07/P1-08, де реалізація вже запушена, а статус досі TODO) в
> єдиний план готовності до заміни старого сайту на `sibway.com.ua`, без
> внесення змін. Окремо винести власнику пропозицію оновити
> `docs/PROJECT_TODO.md` під фактичний стан цих трьох пунктів.

Обрано саме цю дію: з попереднього handoff-запису додався ще один цикл
реалізації поверх завершеного privacy-циклу — deployment-процес,
публікація `terms.html`, service-aware CTA-роутинг, D-010, D-011.
Природний наступний крок — не чергова точкова правка, а зведення
критеріїв готовності до реального deploy, тепер з урахуванням і нового
`docs/DEPLOYMENT.md`-процесу. `.DS_Store` housekeeping вирішено
(`183a016`) — більше не окрема задача.

## 10. Відкриті питання (розбіжності джерел)

### Закрито

- `docs/CHANGELOG.md` — застарілий запис 2026-08-20 про
  React/Vite/TypeScript вирішено через `764d7cb`: додано коригувальний
  запис 2026-09-04; історичний запис навмисно лишено, але він не є чинною
  настановою.

### Відкрито

- `docs/PROJECT_TODO.md`, P1-08 (`contacts.html` CRO), досі має статус
  TODO, хоча роботу реалізовано й запушено (`749e4f3`, `d8b0d13`).
  Потрібне окреме рішення власника: оновити статус у `PROJECT_TODO.md`
  або залишити TODO до формального прийняття CRO-пакета.
- `docs/PROJECT_TODO.md`, P1-07 (`privacy.html`), досі має статус TODO,
  хоча content-sync реалізовано й запушено (`55a649b`, `217a249`,
  `0656f99`). Той самий патерн, що й P1-08 — потребує окремого рішення
  власника.
- GA4 (`G-CN7867WYLT`) та інформаційний cookie-notice реалізовані в Git
  (`6ee3c22`), але **не верифіковані на продакшні** — новий сайт ще не
  замінив старий на `sibway.com.ua`. Post-deploy чекліст зафіксовано в
  `docs/COOKIE_INVENTORY.md`.
- UA/EN/PL Privacy Policy content синхронізовано в Git, але **не пройшов
  юридичного рев'ю** — усі чотири internal source-of-truth документи
  прямо позначені як «не юридичний висновок». Потрібне окреме залучення
  юриста перед тим, як вважати текст остаточним.
- `docs/PROJECT_TODO.md`, P1-06 (`terms.html`), досі має статус TODO
  «блокується відсутністю юртексту», хоча повний юридичний текст
  опубліковано й запушено в UA/EN/PL (`a5be88f`). На відміну від
  Privacy Policy, немає internal source-of-truth документа чи запису в
  `docs/DECISIONS.md`, що підтверджує саме цей текст як погоджений і
  юридично перевірений — найсильніша з трьох P1-0x розбіжностей.
- D-011 (стабілізація шрифтів і CTA, `docs/DECISIONS.md`) прямо вимагає
  обов'язкову ручну post-deploy перевірку — Windows 11, Chrome 128,
  масштаб 100%, `uk/index.html`, футер — і перевірку клавіатурного
  фокуса на CTA в hero та cta-band. Жодна з цих перевірок ще не
  виконана.
